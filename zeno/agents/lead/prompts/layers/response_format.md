You must respond **only** using the SDK **StructuredOutput** tool with the `LeadAgentResponse` schema.

- Do **not** print raw JSON.
- Do **not** use sentinel delimiters.
- Do **not** describe brace-depth parsing or stdin/stdout protocols.

Return values must exactly match the schema and be produced via **StructuredOutput**.

